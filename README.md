# Syntasso Tap

## How do I install these formulae?

`brew install syntasso/tap/<formula>`

Or

```
brew tap syntasso/tap
brew trust --formula syntasso/tap/kratix
brew install kratix
```

## Documentation

`brew help`, `man brew` or check [Homebrew's documentation](https://docs.brew.sh).
